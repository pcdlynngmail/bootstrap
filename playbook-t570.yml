---

- name:
  hosts: localhost
  gather_facts: no
  
  tasks:

    - name: Directory structure
      file:
        path: /code
        state: directory
        mode: 0755
      
      #   path: /inbox
      #   state: directory
      #   mode: 0755
      # file:
      #   path: /repo
      #   state: directory
      #   mode: 0755
      # file:
      #   path: /sync
      #   state: directory
      #   mode: 0755
      # file:
      #   path: /temp
      #   state: directory
      #   mode: 0755
      # file:
      #   path: /tools
      #   state: directory
      #   mode: 0755
